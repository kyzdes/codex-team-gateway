#!/usr/bin/env bash
# Запускается через sudo от имени пользователя agent.
# Единственная задача — выставить umask, чтобы файлы, созданные агентом,
# оставались доступными шлюзу по группе, и передать управление команде.
umask 002
exec "$@"
